<div>
    
     <main>
         <section>
            {{>title data="My education" big=false}}
            
        <ul>
            {{#each main.education}}
               <li>{{name}} {{#unless isEnd}}(ще в процесі){{/unless}}</li>
            {{/each}}
    </ul>
    </section>
    <section>
        {{>title data="My certificates" big=true}}
        <ul>
            {{#each main.certificates}}
            <li>{{name}} ({{id}}) </li>
            {{/each}}
        </ul>
    </section>
    </main>

    </div>